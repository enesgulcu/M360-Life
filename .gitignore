# M360 Life — git ignore (Workbench addon)

# OS / editor
Thumbs.db
Desktop.ini
.DS_Store
*.swp
*~
.vscode/
.idea/

# Cursor local (isteğe bağlı; konuşma notları repoda tutulabilir)
# .cursor/

# Secrets
.env
.env.*
*.pem
credentials.json

# Workbench / build junk
build/
*.tmp
*.bak
*.log
~$*

# Workbench regenerated
resourceDatabase.rdb
*.rdb

# Node / Next (platform apps — GitHub kökünde)
node_modules/
.next/
apps/**/.env
apps/**/.env.local
packages/**/.env

