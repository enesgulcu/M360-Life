export default function HomePage() {
  return (
    <main style={{ fontFamily: "system-ui", padding: 24 }}>
      <h1>M360 game-api</h1>
      <p>Yerel lab API. Health: <code>/api/health</code> · Jobs: <code>/api/jobs</code></p>
    </main>
  );
}
